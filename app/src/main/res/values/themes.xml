<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BlueSquareSpeedometer" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="baseTextColor">#000000</item>
        <item name="backgroundColor">#ffffff</item>
        <item name="alertBackgroundColor">#ff4444</item>
        <item name="android:textAllCaps">false</item>

        <!--
          Since Android 15, edge-to-edge is enforced.
          However, this does not look nice with Blue Line Console design.
          This is to be removed, so we must consider how to deal with the new design requirement.
          For now, placing this workaround API.
           -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <style name="Theme.BlueSquareSpeedometerFullScreenNight" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="baseTextColor">#ffffff</item>
        <item name="backgroundColor">#000000</item>
        <item name="alertBackgroundColor">#dd0000</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>